'use client';
import { useEffect, useState } from 'react';
type Preferences={appearance:'system'|'light'|'comfortable';textSize:'default'|'large';contrast:boolean;reduceMotion:boolean;density:'comfortable'|'compact'};
const defaults:Preferences={appearance:'comfortable',textSize:'default',contrast:false,reduceMotion:false,density:'comfortable'};
function readPreferences():Preferences{if(typeof window==='undefined')return defaults;try{const saved=localStorage.getItem('reliable-medical-preferences');return saved?{...defaults,...JSON.parse(saved)}:defaults;}catch{return defaults;}}
export function SettingsPreferences(){const [prefs,setPrefs]=useState<Preferences>(readPreferences);useEffect(()=>{localStorage.setItem('reliable-medical-preferences',JSON.stringify(prefs));document.documentElement.dataset.preferences=prefs.appearance;document.documentElement.classList.toggle('pref-large-text',prefs.textSize==='large');document.documentElement.classList.toggle('pref-high-contrast',prefs.contrast);document.documentElement.classList.toggle('pref-reduce-motion',prefs.reduceMotion);document.documentElement.classList.toggle('pref-compact',prefs.density==='compact');},[prefs]);const update=(patch:Partial<Preferences>)=>setPrefs((v)=>({...v,...patch}));return <div className="space-y-4"><section className="rounded-2xl border border-slate-200 bg-slate-50 p-5"><h2 className="font-semibold text-slate-900">Appearance</h2><div className="mt-3 flex flex-wrap gap-2">{(['system','light','comfortable'] as const).map(v=><button key={v} type="button" onClick={()=>update({appearance:v})} className={`rounded-lg border px-3 py-2 text-sm capitalize ${prefs.appearance===v?'border-blue-500 bg-blue-50 text-blue-800':'border-slate-200 bg-white text-slate-600'}`}>{v}</button>)}</div></section><section className="rounded-2xl border border-slate-200 bg-slate-50 p-5"><h2 className="font-semibold text-slate-900">Accessibility</h2><div className="mt-3 grid gap-3 sm:grid-cols-2"><label className="flex items-center gap-2 text-sm text-slate-700"><input type="checkbox" checked={prefs.textSize==='large'} onChange={(e)=>update({textSize:e.target.checked?'large':'default'})}/> Larger text</label><label className="flex items-center gap-2 text-sm text-slate-700"><input type="checkbox" checked={prefs.contrast} onChange={(e)=>update({contrast:e.target.checked})}/> Higher contrast</label><label className="flex items-center gap-2 text-sm text-slate-700"><input type="checkbox" checked={prefs.reduceMotion} onChange={(e)=>update({reduceMotion:e.target.checked})}/> Reduce motion</label><label className="flex items-center gap-2 text-sm text-slate-700"><input type="checkbox" checked={prefs.density==='compact'} onChange={(e)=>update({density:e.target.checked?'compact':'comfortable'})}/> Compact density</label></div></section><p className="text-xs text-slate-500">Preferences are stored locally in this browser and do not affect case data.</p></div>}
